# Security

请勿在 Issue、截图或日志中提交 API Key、AccessKey、Cookie、浏览器 Profile、真实业务数据或本机绝对路径。

发现安全问题时，请进入仓库的 **Security → Advisories → Report a vulnerability** 私密报告，不要在公开 Issue 中披露。如果仓库尚未启用该入口，请只创建一条不含漏洞细节的 Issue，询问维护者如何建立私密联系。

## 支持版本

安全更新只提供给最新的 GitHub Release。Beta 版本不承诺长期维护，但确认的高风险问题会优先修复。

发布前请执行 `docs/隐私与发布检查清单.md`。
